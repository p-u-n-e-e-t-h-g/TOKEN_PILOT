# Routing

The routing layer classifies prompts, estimates cost and token usage, then selects a provider/model based on configured priorities.

Initial routing signals:

- Prompt complexity
- Estimated tokens
- Provider availability
- Cost preference
- Latency preference

